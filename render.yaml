services:
  - type: web
    name: amritsari-kulcha-system
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    autoDeploy: true
    healthCheckPath: /api/catalog/menu
    envVars:
      - key: DATABASE_URL
        sync: false
